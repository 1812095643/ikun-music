# Android 第一阶段远程主接口地址（必填）
# 用途：首页、搜索、歌单、歌曲详情、歌词等网易云接口请求。
# 要求：必须是手机可以访问的远程 HTTP/HTTPS 地址，不能填 127.0.0.1 或 localhost。
# 复制后的 .env.android 已被 .gitignore 忽略，请不要把真实接口地址提交到仓库。
VITE_API = https://你的远程主接口地址

# Android 第一阶段远程播放解析接口地址（必填）
# 用途：官方播放地址不可用时，请求 /music 做基础播放兜底。
# 要求：必须是手机可以访问的远程 HTTP/HTTPS 地址，不能填 127.0.0.1 或 localhost。
# 复制后的 .env.android 已被 .gitignore 忽略，请不要把真实接口地址提交到仓库。
VITE_API_MUSIC = https://你的远程播放解析接口地址
