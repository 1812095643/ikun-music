# ikun-music 项目分析文档

## 1. 项目是做什么的

`ikun-music` 是一个以 `Tauri` 为主桌面壳的音乐播放器项目，目标不是只做一个普通在线播放页面，而是把下面几类能力整合到一个桌面应用里：

- 在线音乐浏览与播放：首页、搜索、歌单、专辑、排行榜、MV、播客、用户页等。
- 多音源补链：当默认接口拿不到稳定播放地址时，按策略切换到其他音源补齐可播 URL。
- 本地音乐管理：扫描本地文件夹、解析元数据、缓存封面和歌词、直接播放本地文件。
- 桌面增强能力：系统托盘、精简模式、独立桌面歌词窗口、快捷键、下载、更新、窗口控制。
- 内置本地服务：应用启动时拉起本地音乐 API 服务，给前端提供补链、扫描、下载等桌面场景能力。

一句话概括：这是一个“桌面播放器 + 多音源补链平台 + 本地音乐管理器”的组合型项目。

## 2. 先把几个术语说清楚

- `主窗口`：承载正常业务页面的应用主界面，对应 `/` 下的大部分路由。
- `精简模式`：主窗口切到 `/mini` 路由后的迷你播放器形态，不是新应用实例，而是同一个主窗口切布局。
- `桌面歌词窗口`：独立的歌词窗口，对应 `/lyric` 路由，只负责歌词显示和轻量交互。
- `托盘面板`：点击托盘后弹出的轻量控制面板，对应 `/tray-panel` 路由。
- `补链`：给歌曲补出真正可播放的音频 URL。
- `兼容层`：把 Tauri 的窗口、文件、事件、HTTP、存储能力适配成渲染层熟悉的 `window.api` 调用方式。
- `isElectron`：这是仓库里的历史命名，它现在更接近“桌面运行时标记”，并不代表项目主栈仍然是 Electron。

## 3. 技术栈结论

### 3.1 主技术栈

- 前端：`Vue 3`、`Pinia`、`Vue Router`、`Naive UI`、`Vite`
- 音频播放：`Howler.js`
- 桌面壳：`Tauri 2`
- 桌面能力实现：`Rust` + Tauri 插件
- 内置本地服务：`Node.js` 脚本运行时 + `netease-cloud-music-api-alger`

### 3.2 为什么说主栈是 Tauri，不是 Electron

可以直接从两个入口判断：

- [package.json](D:\lishuyi\STUDY\ikun-music\package.json)
  `dev`、`build`、`tauri:dev`、`tauri:build` 都走 `tauri dev` / `tauri build`
- [src-tauri/tauri.conf.json](D:\lishuyi\STUDY\ikun-music\src-tauri\tauri.conf.json)
  已经配置了前端开发地址、构建前命令、打包资源和主窗口参数

结论很明确：

- 当前开发与打包主线是 `Tauri`
- `src/main`、`src/preload`、`electron.vite.config.ts` 这一套仍然保留，主要用于历史兼容、行为对照和桥接适配

所以最准确的说法是：

`这个项目的主技术栈是 Tauri，Electron 代码是兼容层和历史实现，不是当前主线。`

## 4. 整体架构怎么分

### 4.1 渲染层

- 入口：[src/renderer/main.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\main.ts)
- 根组件：[src/renderer/App.vue](D:\lishuyi\STUDY\ikun-music\src\renderer\App.vue)
- 路由：[src/renderer/router/index.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\router\index.ts)
- 状态管理：[src/renderer/store/index.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\store\index.ts)

渲染层负责：

- 页面展示
- 播放器状态管理
- 歌词同步
- 用户设置
- 与桌面能力层通信

### 4.2 Tauri 桌面层

- 入口：[src-tauri/src/lib.rs](D:\lishuyi\STUDY\ikun-music\src-tauri\src\lib.rs)

Tauri 层负责：

- 创建和控制主窗口、精简模式、托盘面板、桌面歌词窗口
- 建立系统托盘
- 注册命令给前端调用
- 启动内置音乐 API 服务
- 处理桌面歌词位置持久化、窗口几何恢复、鼠标穿透等能力

### 4.3 兼容桥接层

- 入口：[src/renderer/utils/tauriElectronCompat.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\utils\tauriElectronCompat.ts)

这一层的作用是：

- 对渲染层暴露统一的 `window.api`
- 在 Tauri 环境里把 `invoke`、`emitTo`、`listen`、文件系统、HTTP、Store 等能力包装成旧接口形状
- 在浏览器环境里提供有限兜底

它的存在让渲染层大部分业务代码不需要直接区分 Tauri 和旧 Electron 风格接口。

### 4.4 历史 Electron 层

- 主进程目录：[src/main](D:\lishuyi\STUDY\ikun-music\src\main)
- preload 目录：[src/preload](D:\lishuyi\STUDY\ikun-music\src\preload)

这一层仍然有参考价值，尤其是：

- 对照 Tauri 行为是否补齐
- 理解旧的窗口事件和 IPC 语义
- 兼容已有渲染层调用方式

## 5. 关键目录职责

- [src/renderer](D:\lishuyi\STUDY\ikun-music\src\renderer)：前端页面、组件、路由、Pinia Store、业务逻辑
- [src-tauri](D:\lishuyi\STUDY\ikun-music\src-tauri)：Tauri Rust 代码、命令注册、窗口控制、打包配置
- [src/main](D:\lishuyi\STUDY\ikun-music\src\main)：历史 Electron 主进程实现和桌面侧业务能力
- [src/preload](D:\lishuyi\STUDY\ikun-music\src\preload)：历史 preload 桥接
- [src/i18n](D:\lishuyi\STUDY\ikun-music\src\i18n)：多语言
- [resources](D:\lishuyi\STUDY\ikun-music\resources)：图标、HTML 等资源
- [scripts](D:\lishuyi\STUDY\ikun-music\scripts)：构建辅助脚本
- [docs](D:\lishuyi\STUDY\ikun-music\docs)：专项说明文档

## 6. 关键功能是怎么实现的

### 6.1 应用启动链路

前端启动链路：

1. [src/renderer/main.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\main.ts) 创建 Vue 应用、挂载 Pinia、Router、i18n。
2. 如果桌面层提前写入了 `window.__IKUN_INITIAL_ROUTE__`，前端会先跳到指定路由再挂载，例如 `/lyric`、`/tray-panel`。
3. [src/renderer/App.vue](D:\lishuyi\STUDY\ikun-music\src\renderer\App.vue) 根据当前窗口类型初始化播放器、托盘状态同步、快捷键等逻辑。

Tauri 启动链路：

1. [src-tauri/src/lib.rs](D:\lishuyi\STUDY\ikun-music\src-tauri\src\lib.rs) 的 `setup` 里先创建托盘。
2. 同一个 `setup` 里启动本地音乐 API 服务。
3. 通过 `invoke_handler` 暴露窗口控制、歌词窗控制、托盘控制、音乐 API 启动等命令给前端。

### 6.2 播放主链路

核心文件：

- [src/renderer/store/modules/player.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\store\modules\player.ts)
- [src/renderer/store/modules/playerCore.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\store\modules\playerCore.ts)
- [src/renderer/services/audioService.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\services\audioService.ts)
- [src/renderer/services/playbackRequestManager.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\services\playbackRequestManager.ts)
- [src/renderer/hooks/MusicHook.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\hooks\MusicHook.ts)

实现思路：

1. `player.ts` 是聚合层，把播放、播放列表、收藏、定时关闭等子 store 组合成统一播放器入口。
2. 用户点歌后，`playerCore.handlePlayMusic()` 先创建 `requestId`，同时取消旧请求。
3. `playbackRequestManager` 把“当前请求”作为唯一有效请求，防止快速切歌时旧异步结果覆盖新歌曲状态。
4. `handlePlayMusic()` 先更新当前歌曲和播放意图，再异步补齐歌曲详情。
5. 歌词候选搜索和封面取色在后台继续执行，不阻塞首帧播放。
6. `preloadService` 负责预加载音频；`audioService.play()` 负责真正把音频交给 `Howler` 播放。
7. `checkPlaybackState()` 会在播放开始后做延迟校验，发现假播放、无声播放或临时直链失效时自动重试。
8. `MusicHook.ts` 负责把播放进度、歌词索引、桌面歌词同步、播放结束后的行为统一串起来。

这里最关键的设计点有两个：

- `requestId` 机制：解决点歌、切歌、歌词异步回写之间的竞态问题。
- “先出声，再补信息”：歌词、主题色、候选歌词搜索不阻塞播放主链路。

### 6.3 多音源补链

核心文件：

- [src/renderer/api/musicParser.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\api\musicParser.ts)
- [src/renderer/services/LxMusicSourceRunner.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\services\LxMusicSourceRunner.ts)
- [src/main/unblockMusic.ts](D:\lishuyi\STUDY\ikun-music\src\main\unblockMusic.ts)

补链流程：

1. `MusicParser.parseMusic()` 先读取全局设置和歌曲级音源配置。
2. 先查缓存，命中则直接返回。
3. 按策略优先级依次尝试不同补链方案。
4. 成功后缓存稳定 URL，失败则写入失败缓存，避免短时间内重复打坏接口。

当前策略顺序：

1. `LxMusicStrategy`
2. `CustomApiStrategy`
3. `GDMusicStrategy`
4. `UnblockMusicStrategy`

这部分实现里有几个值得注意的点：

- 临时直链不会长时间落盘缓存，避免过期 URL 被反复复用。
- 失败结果也会短暂缓存，减少无效重试。
- LX Music 脚本不是直接在主线程执行，而是放到 `Worker` 沙箱里跑，风险更低。

### 6.4 本地音乐扫描与缓存

核心文件：

- [src/renderer/store/modules/localMusic.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\store\modules\localMusic.ts)
- [src/main/modules/localMusicScanner.ts](D:\lishuyi\STUDY\ikun-music\src\main\modules\localMusicScanner.ts)

实现方式：

1. 前端 store 保存用户配置过的本地音乐文件夹路径。
2. 桌面层递归扫描文件夹，返回 `文件路径 + modifiedTime`。
3. 前端拿已有缓存做增量比对，只重新解析新增或修改过的文件。
4. 元数据解析使用 `music-metadata`，提取标题、歌手、专辑、时长、封面、歌词。
5. 最终结果写入 `IndexedDB`，前端页面直接从缓存读取。

这条链路的核心优点是：

- 文件系统递归扫描放在桌面能力层处理。
- 元数据缓存放在 `IndexedDB`，避免每次启动都全量重扫。
- 用 `modifiedTime` 做增量刷新，成本明显更低。

### 6.5 托盘、精简模式和桌面歌词

核心文件：

- [src/renderer/App.vue](D:\lishuyi\STUDY\ikun-music\src\renderer\App.vue)
- [src/renderer/hooks/MusicHook.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\hooks\MusicHook.ts)
- [src-tauri/src/lib.rs](D:\lishuyi\STUDY\ikun-music\src-tauri\src\lib.rs)
- [src/main/lyric.ts](D:\lishuyi\STUDY\ikun-music\src\main\lyric.ts)

托盘：

- 前端把当前歌曲、播放状态、音量等信息主动同步给桌面层。
- Tauri 层负责创建原生托盘和托盘面板窗口。
- 托盘按钮和面板动作最终还是回到主窗口的播放器 store 执行，主窗口是唯一真源。

精简模式：

- 路由上切到 `/mini`，不是新播放器实例。
- Tauri 层会在进入精简模式前记住主窗口尺寸、位置、最大化状态。
- 恢复时再一次性把主窗口几何状态还原，避免用户原本布局丢失。

桌面歌词：

- 独立窗口使用 `/lyric` 路由。
- 主窗口持续向歌词窗口推送歌词内容、当前行、播放状态和控制事件。
- 歌词窗口支持鼠标穿透、锁定、拖动、主题色、显示模式、翻译开关等个性化配置。
- 桌面层会持久化歌词窗口尺寸和位置，恢复前先校验坐标是否仍落在当前显示器范围内。
- 生命周期通过 `ready`、`closed`、`control-back` 等事件与主窗口同步。
- 当前改动还额外给生命周期事件带上时间戳，用来忽略“旧窗口晚到的 closed 事件覆盖新窗口状态”这类竞态。

### 6.6 Tauri 兼容桥接层

核心文件：

- [src/renderer/utils/tauriElectronCompat.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\utils\tauriElectronCompat.ts)

它做了几件关键事情：

- 用 `window.api` 形式统一暴露桌面能力。
- 把 Tauri 的 `invoke`、事件系统、插件 FS、插件 HTTP、插件 Store 包装成旧接口风格。
- 在桌面环境里负责启动本地音乐 API 服务并维护端口。
- 给 POST 请求拼接唯一参数，绕过本地服务全局缓存误命中的问题。
- 在浏览器环境里退化到 `localStorage` 和有限的前端能力兜底。

对维护者来说，这一层是“渲染层和桌面层的接口契约”。

另外，当前兼容层也承担一部分本地调试兜底职责：

- 在非 Tauri 浏览器环境下，部分桌面命令会退化成本地事件或路由切换。
- 这样可以直接在本地前端调试页里验证精简模式和桌面歌词的前端交互，不必每次都依赖真实桌面壳。

### 6.7 内置音乐 API 服务

核心文件：

- [src-tauri/src/lib.rs](D:\lishuyi\STUDY\ikun-music\src-tauri\src\lib.rs)
- [src/main/server.ts](D:\lishuyi\STUDY\ikun-music\src\main\server.ts)

当前主线实现：

1. Tauri 层在启动时释放内置 Node 运行时压缩包。
2. 自动定位 `node` 和 `alger-music-api.js`。
3. 尝试启动本机回环地址上的音乐 API 服务。
4. 如果目标端口被占用，会自动顺延寻找可用端口。

这部分的价值在于：

- 前端不依赖外部常驻服务，开箱即用。
- 打包后依旧能带着运行时工作。
- 只监听 `127.0.0.1`，默认不暴露到局域网。

## 7. 建议优先阅读的文件

如果你后面要继续维护这个项目，建议按下面顺序看：

1. [src/renderer/store/modules/playerCore.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\store\modules\playerCore.ts)
2. [src/renderer/services/audioService.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\services\audioService.ts)
3. [src/renderer/api/musicParser.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\api\musicParser.ts)
4. [src/renderer/hooks/MusicHook.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\hooks\MusicHook.ts)
5. [src/renderer/utils/tauriElectronCompat.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\utils\tauriElectronCompat.ts)
6. [src-tauri/src/lib.rs](D:\lishuyi\STUDY\ikun-music\src-tauri\src\lib.rs)
7. [src/renderer/store/modules/localMusic.ts](D:\lishuyi\STUDY\ikun-music\src\renderer\store\modules\localMusic.ts)
8. [src/main/modules/localMusicScanner.ts](D:\lishuyi\STUDY\ikun-music\src\main\modules\localMusicScanner.ts)

## 8. Android App 适配第一阶段

### 8.1 阶段目标

Android 第一阶段只追求 Tauri Android 能启动，并让首页、搜索、播放、歌词基础链路可用。
本阶段不做 iOS、不做后台播放、不做通知栏控制、不做应用商店发布。

### 8.2 当前适配策略

- 前端继续复用现有 Vue、Pinia、Vue Router 和移动端布局。
- 手机端主界面走 [src/renderer/layout/MobileLayout.vue](D:\lishuyi\STUDY\ikun-music\src\renderer\layout\MobileLayout.vue)。
- 移动端播放栏走 [src/renderer/components/player/MobilePlayBar.vue](D:\lishuyi\STUDY\ikun-music\src\renderer\components\player\MobilePlayBar.vue)。
- 移动端全屏歌词走 [src/renderer/components/lyric/MusicFullMobile.vue](D:\lishuyi\STUDY\ikun-music\src\renderer\components\lyric\MusicFullMobile.vue)。
- Android 端先按移动端 WebView 运行，不复用桌面托盘、独立歌词窗、精简窗口和窗口拖拽能力。

### 8.3 第一版已完成进度

- 新增运行时判定：`isAndroidRuntime` 和 `isDesktopRuntime`。
- Android 请求参数按 `mobile` 设备类型发送，避免被当成桌面 `pc`。
- Android 启动时不等待桌面内置 Node 音乐 API 服务。
- Android 运行时不初始化桌面托盘状态同步、托盘控制、精简窗口事件和桌面更新弹窗。
- Tauri Rust 入口在 `mobile` 编译目标下跳过托盘创建和内置 Node 音乐 API 自动启动。
- Tauri JS 依赖版本已收敛到当前 Rust 侧 Tauri 版本线，减少移动端命令执行前的版本检查风险。

### 8.4 后续仍需完成

- 初始化完整 Android 工程：执行 `npx tauri android init` 后生成真实 Android 项目结构。
- 配置 Android 本地开发环境：Rust/Cargo、Android Studio、SDK、NDK、JDK 和 Android 目标。
- 在真机或模拟器上执行 `npx tauri android dev`，验证应用能进入首页。
- 验证搜索：移动端搜索页能输入关键词、展示结果并进入歌曲播放。
- 验证播放：点击歌曲后能创建 Howler 播放实例，并能暂停、继续、切歌。
- 验证歌词：播放页能打开移动端全屏歌词，歌词能随播放进度更新。
- 梳理 Android 文件权限和下载策略，本阶段可以先不开放本地音乐扫描和下载能力。
- 判断多音源补链是否需要改成远端接口或纯前端策略，避免依赖桌面本地 Node 服务。

### 8.5 自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npx tauri info` 的 Tauri JS/Rust 版本一致性检查。
- 当前本机还需要安装 Rust/Cargo 后，才能继续执行 Android 初始化和真机运行验证。

### 8.6 第二版适配进度

- Rust 侧已把托盘、桌面歌词窗口、精简窗口、内置 Node 音乐 API 等桌面专属能力用 `#[cfg(not(mobile))]` 隔离，减少 Android 编译目标误编译桌面 API 的风险。
- Android `mobile` 编译目标下保留同名 Tauri 命令的最小安全返回，避免前端兼容层调用桌面命令时因为命令缺失影响启动。
- Android 启动阶段仍不自动创建系统托盘、不启动内置 Node 服务、不创建独立歌词窗口；首页、搜索、播放、歌词仍走移动端 WebView 主链路。
- 本轮未修改 `src-tauri/Cargo.toml`，避免把本地已有行尾状态改动混入 Android 适配提交。

### 8.7 第二版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npx vite build`；构建仍有既有 chunk 体积和动态导入提示，不影响本轮 Android 启动级适配判断。
- 已执行 `npx tauri info`，当前 Tauri JS/Rust 版本检查可正常输出；本机仍未安装 `rustc`、`Cargo`、`rustup`，因此暂不能执行 `npx tauri android init/dev` 和 Android 真机启动验证。

### 8.8 第三版适配进度

- Android 主 API 请求不再因为 Tauri 兼容层存在 `window.electron` 而误打到 `127.0.0.1:30488`，移动端改走 `VITE_API` 远端接口。
- 酷我、外部音源请求和歌词候选请求已改为仅桌面运行时才使用本地 HTTP 代理；Android 端走普通 `fetch` 或远端请求链路。
- Android 第一阶段继续保持不启动内置 Node 音乐 API，首页、搜索、播放、歌词基础链路不再依赖桌面本地服务启动状态。

### 8.9 第三版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npx vite build`；构建仍有既有 chunk 体积和动态导入提示。
- 受本机缺少 `rustc`、`Cargo`、`rustup` 限制，仍无法执行 `npx tauri android init/dev` 做 Android 原生工程和真机启动验证。

### 8.10 第四版适配进度

- 新增 Android 专属 Tauri 配置 `src-tauri/tauri.android.conf.json`，Android 构建时用 `npm run build:web` 覆盖桌面端 `npm run build:desktop`，避免移动端误执行内置 Node 音乐 API 运行时准备流程。
- 新增 Android 目标脚本：`android:init`、`android:dev`、`android:build`、`android:run`，后续做 Android 第一阶段验证时优先使用这些脚本，不再混用桌面 `tauri build`。
- `android:build` 默认生成 APK，当前命令为 `tauri android build --apk true`。

### 8.11 第四版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积和动态导入提示。
- 已通过 `npm run android:dev -- --help` 和 `npm run android:build -- --help` 的命令解析检查。
- 已执行 `npx tauri info`，本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能继续执行 Android 原生初始化、编译和真机启动验证。

### 8.12 第五版适配进度

- 将 Android 第一阶段容易误触发的桌面专属入口继续收敛到 `isDesktopRuntime`：设置持久化 IPC、系统字体初始化、应用更新状态、顶部下载/更新/缩放/重启入口、本地音乐菜单、歌曲下载按钮、批量下载、右键下载菜单、桌面歌词窗口同步都不再因为 Android Tauri 注入兼容对象而被当成桌面功能。
- 播放和歌词主链路继续保留移动端能力：Android 仍走移动端布局、移动端播放条和全屏歌词页；仅独立桌面歌词窗口、托盘同步、磁盘缓存下载等桌面能力被隔离。
- 搜索页和歌单页的播放全部、单曲播放、列表展示继续保留；批量选择/下载这类桌面下载入口在 Android 第一阶段不开放，避免触发不可用的文件系统和桌面 IPC 能力。

### 8.13 第五版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已通过 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.14 第六版适配进度

- Android 离线状态不再自动跳转 `/local-music`，避免第一阶段未开放本地音乐扫描时进入桌面专属页面；桌面端仍保留离线跳本地音乐的原有行为。
- 搜索建议从 `isElectron` 改为 `isDesktopRuntime` 判断，Android Tauri 注入兼容对象时不再调用桌面 IPC 代理，改走移动端可用的远端兜底接口。
- 播放解析主入口从 `isElectron` 改为 `isDesktopRuntime` 判断，Android 不再进入桌面多音源解析策略和 `window.api.unblockMusic` 链路，优先走普通 `/music` 请求保证基础播放链路。
- 歌词候选的磁盘缓存读写仅桌面启用，Android 继续走网络歌词接口，避免调用桌面歌词缓存 IPC。
- 播放状态同步、定时关闭通知和下载进度监听已增加桌面运行时门禁，Android 第一阶段不触发桌面通知、托盘或下载 IPC。

### 8.15 第六版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npx vite build --debug`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- `npm run build:web` 首次执行时构建进程长时间未退出并超时，清理本次残留构建进程后，分段执行类型检查与 Vite 构建均已通过。
- 已通过 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.16 第七版适配进度

- 应用内快捷键初始化从 `isElectron` 收敛到 `isDesktopRuntime`，Android Tauri 启动时不再注册桌面全局快捷键监听，也不再同步读取桌面快捷键配置。
- 首页歌单悬浮曲目预览和专辑曲目预加载改为仅桌面运行时启用，Android 首页只保留列表展示、点击进入详情和播放入口，减少首屏额外请求和桌面交互依赖。
- 音频 EQ 初始化从 `isElectron` 收敛到 `isDesktopRuntime`，Android 第一阶段播放链路默认跳过桌面 WebAudio/EQ 图，优先保证基础播放稳定。

### 8.17 第七版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已通过 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.18 第八版适配进度

- Android 请求配置读取不再因为 Tauri 兼容层注入 `window.electron` 而调用桌面同步 IPC，`getSetData()` 仅在 `isDesktopRuntime` 下读取桌面 store；Android 继续使用 Pinia 设置状态。
- 播放服务读取设置时同样收敛到桌面运行时才访问 `window.electron.ipcRenderer.sendSync('get-store-value', 'set')`，Android 播放链路回退读取 `localStorage`，避免启动或播放时触发桌面 IPC。
- 本轮只处理全局设置读取入口，不扩展后台播放、通知栏控制、iOS 或应用商店发布范围，继续保持 Android 第一阶段“能启动、能搜索、能播放、能看歌词”的目标边界。

### 8.19 第八版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 环境信息可正常输出，但本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.20 第九版适配进度

- 应用启动入口继续从 `isElectron` 收敛到 `isDesktopRuntime`，Android Tauri 兼容层存在时不再注册托盘控制、托盘面板、迷你模式语言监听和桌面歌曲同步事件。
- 标题栏窗口控制按钮改为仅桌面运行时调用最小化、最大化、精简窗口、托盘隐藏、关闭和拖拽 API，避免 Android 平板模式或异常布局下误触发桌面窗口能力。
- 页面缩放组合式 API 增加桌面运行时和 `window.ipcRenderer` 能力判断；Android 第一阶段不再调用桌面缩放 IPC，只保留前端状态兜底。

### 8.21 第九版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 环境信息可正常输出，但本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.22 第十版适配进度

- 迷你播放栏的恢复主窗口、调整迷你窗尺寸、迷你窗歌单抽屉跳转和展开完整播放器逻辑继续收敛到 `isDesktopRuntime`，Android 即使误进入 `/mini` 路由也不再调用桌面窗口 API。
- 歌手跳转在迷你模式下仅桌面运行时才走“恢复主窗口后跳转”的链路，Android 继续使用普通路由跳转，避免依赖桌面恢复窗口能力。
- PC 播放栏里的高级控制菜单仅桌面运行时展示，Android 第一阶段不开放 EQ、定时关闭、音效面板等非基础能力；移动端主链路仍保留首页、搜索、播放和歌词。
- 本轮确认下载按钮组件自身已由 `isDesktopRuntime` 保护，Android 不会显示歌曲下载入口；本地音乐扫描入口仍不纳入第一阶段。

### 8.23 第十版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 环境信息可正常输出，但本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.24 第十一版适配进度

- 设置页桌面专属分区从 `isElectron` 收敛到 `isDesktopRuntime`，Android Tauri 兼容层存在时不再显示应用、网络、系统等桌面设置分区。
- 下载管理、下载路径、快捷键、远程控制、磁盘缓存、清理下载记录、桌面重启、代理配置、GPU 加速、系统字体、音频设备和桌面音源配置继续限制为桌面运行时，避免 Android 第一阶段误触发桌面 IPC 或文件系统能力。
- 基础设置里的平板模式在 Android 继续可见，便于后续验证移动端/平板布局切换；Android 播放设置只保留音质、自动播放、淡入淡出等纯前端基础选项。
- 关于页更新检查改为仅桌面运行时使用桌面更新器；Android 第一阶段不使用桌面更新弹窗和打开更新页能力。

### 8.25 第十一版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 环境信息可正常输出，但本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.26 第十二版适配进度

- 文件目录选择和打开工具增加桌面运行时兜底，Android 即使误触发下载路径或缓存路径相关动作，也不会调用桌面文件选择 IPC。
- 下载抽屉组件仅桌面运行时渲染和监听下载进度事件，Android 第一阶段不挂载下载 IPC 监听，也不暴露下载页面入口。
- 免责声明退出逻辑改为仅桌面运行时调用 `quitApp`，Android 下不再把 Tauri 兼容层当作桌面退出能力。
- Web 安装提示改为 Tauri 应用内统一不展示，避免 Android App 内出现浏览器安装提示；移动端首页、搜索、播放和歌词主链路不受影响。

### 8.27 第十二版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 环境信息可正常输出，但本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.28 第十三版适配进度

- 下载管理路由增加 `electronOnly` 标记，保持与菜单层桌面专属过滤一致，Android 第一阶段不再把下载管理作为可访问页面。
- 全局路由守卫增加 Android 兜底：当 Android 直达 `/downloads`、`/local-music`、`/lyric`、`/tray-panel`、`/mini` 或其他桌面专属路由时，统一回到首页。
- 桌面专属路由在非桌面运行时也统一重定向首页，避免 Tauri Android 兼容层注入后绕过菜单过滤进入桌面窗口、迷你模式或本地文件能力页面。
- 本轮只处理路由层入口保护，不新增 iOS、后台播放、通知栏控制、下载、本地音乐扫描或商店发布相关能力。

### 8.29 第十三版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 环境信息可正常输出，但本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.30 第十四版适配进度

- 登录 Cookie 自动获取入口从 `isElectron` 收敛到 `isDesktopRuntime`，Android Tauri 注入运行时对象时不再显示或监听桌面登录窗口 IPC；Token 登录仍保持可用。
- 用户页导入歌单入口、收藏页批量下载入口和 EQ 面板提示改为桌面运行时判断，Android 第一阶段不暴露依赖桌面文件系统、下载或 WebAudio/EQ 桌面增强的入口。
- 快捷键设置弹窗的读取、保存、暂停和恢复桌面快捷键 IPC 也收敛到 `isDesktopRuntime`，避免 Android 误调用桌面快捷键通道。
- 本轮完成后，渲染层业务代码中的旧 `isElectron` 直接判断已收敛完毕，仅保留运行时定义和请求设备类型判断。

### 8.31 第十四版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 环境信息可正常输出，但本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.32 第十五版适配进度

- 应用主启动入口进一步收敛到 Android 第一阶段目标：Android 不再创建托盘面板状态轮询定时器，减少启动后每 500ms 调用桌面托盘同步兜底函数的开销。
- 离线自动跳转 `/local-music` 的监听仅在桌面运行时注册，Android 第一阶段即使断网也不会进入本地音乐扫描页面。
- 桌面音频输出设备变化监听仅在桌面运行时初始化，Android 启动阶段不再触发音频输出设备枚举和切换逻辑。
- 自定义落雪音源 Worker 初始化仅在桌面运行时执行，Android 第一阶段保留远端接口基础播放链路，不在启动时加载桌面增强音源脚本。

### 8.33 第十五版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk 和动态导入提示，不属于本轮 Android 适配新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 环境信息可正常输出，但本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂不能执行 Android 原生初始化、编译和真机启动验证。

### 8.34 第十六版适配进度

- 新增 Android 专属 Tauri capability：`src-tauri/capabilities/android-main.json`，只绑定 `main` 主窗口，只开放 `core:default`、`opener:default`、`http:default`、`store:default`，用于第一阶段首页、搜索、播放、歌词所需的启动、网络请求、配置存储和打开外链能力。
- 桌面 capability `src-tauri/capabilities/main.json` 增加 `platforms: ["windows", "macOS", "linux"]`，继续服务 Windows/macOS/Linux 桌面端的托盘、独立歌词窗口、下载、本地文件、shell 等能力，但不再匹配 Android。
- Android 平台配置 `src-tauri/tauri.android.conf.json` 增加 `app.security.capabilities: ["android-main"]`，Android 构建时优先使用最小权限集合，避免第一阶段继承桌面下载、本地音乐扫描、shell、dialog、托盘面板和独立歌词窗口权限。
- 本轮不新增 iOS、后台播放、通知栏控制、商店发布、本地音乐扫描或下载能力，只收敛 Android 启动期的 Tauri 权限边界。

### 8.35 第十六版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 权限收敛新增问题。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.36 第十七版适配进度

- Tauri Rust 启动链继续收敛移动端原生插件：`opener`、`http`、`store` 作为 Android 第一阶段需要的通用插件保留，`dialog`、`fs`、`shell` 改为仅在非 mobile 编译目标注册。
- Android 第一阶段不再加载桌面文件选择、文件系统和 shell 插件，避免与 `android-main` 最小 capability 的权限边界不一致；Windows/macOS/Linux 桌面端仍保留原插件注册行为。
- 前端 Tauri/Electron 兼容层移除 `@tauri-apps/plugin-dialog` 和 `@tauri-apps/plugin-fs` 的顶层静态导入，改为桌面文件导入场景按需动态加载。
- Android 运行时对目录选择、文件选择、本地音乐扫描、自定义音源文件导入等非第一阶段能力直接返回空结果或取消状态，避免误触发未注册的桌面文件能力；首页、搜索、播放和移动端歌词链路继续走网络请求与前端状态。

### 8.37 第十七版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 插件收敛新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.38 第十八版适配进度

- 兼容层继续收敛 Android 第一阶段不做的下载和本地文件能力：Android 下 `download-music` 不再触发下载落盘流程，避免误调用 `get_downloads_path`、`write_local_file` 等 Rust 文件命令。
- Android 下 `get-downloads-path`、`get-downloaded-music`、`delete-downloaded-music`、`check-file-exists` 和 `save-lyric-file` 均返回空结果、失败状态或不支持提示，不再进入下载目录读取、文件存在性检查、删除文件和保存歌词文件链路。
- 这轮只处理下载/本地文件兜底，不新增 iOS、后台播放、通知栏控制、应用商店发布、本地音乐扫描或下载能力；首页、搜索、播放和移动端歌词展示仍保持原主链路。

### 8.39 第十八版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 下载/本地文件兜底新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.40 第十九版适配进度

- 兼容层内部继续封住桌面内置 Node 音乐 API：Android 下 `ensureMusicApiReady()` 直接返回空结果，不再调用 Rust `start_music_api`，避免误启动桌面本地服务。
- Android 下 `postToMusicApi()` 直接返回空结果，不再访问 `127.0.0.1:{musicApiPort}/alger-tauri/*`，防止本地扫描、元数据解析或 unblockMusic 代理误走桌面服务链路。
- Android 下 `unblock-music` 兼容入口返回 `null`；第一阶段播放解析继续走普通 `/music` 网络接口，优先保证首页、搜索、播放和移动端歌词基础链路可用。
- 本轮不新增下载、本地音乐扫描、后台播放、通知栏控制、iOS 或应用商店发布能力，只补强 Android 第一阶段对桌面本地服务的隔离。

### 8.41 第十九版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 本地音乐 API 隔离新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.42 第二十版适配进度

- 播放解析策略层继续收敛 Android 第一阶段边界：`UnblockMusicStrategy` 在非桌面运行时不再参与可用策略筛选，避免后续配置或调用路径变化时把 Android 播放重新带回桌面 `window.api.unblockMusic` 链路。
- `getUnblockMusicAudio()` 增加桌面运行时和能力判断；Android 下即使误触发该函数，也会直接返回空结果，不再访问桌面内置 Node 音乐服务或本地代理。
- 本轮不新增 iOS、后台播放、通知栏控制、应用商店发布、下载或本地音乐扫描能力，只补强首页/搜索/播放/歌词基础链路中的播放解析兜底。

### 8.43 第二十版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 播放解析策略收敛新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。

### 8.44 第二十一版适配进度

- Android 构建目标模式从普通 `npm run build:web` 收敛为 `npm run build:android:web`，并通过 `vite build --mode android` 明确使用 Android 专用环境变量。
- 新增 `scripts/check-android-env.mjs`，Android 构建前检查 `VITE_API` 和 `VITE_API_MUSIC` 是否存在且不是占位值，避免生产 APK 打包后首页、搜索、播放和歌词请求拿不到远程接口地址。
- 新增 `.env.android.example`，说明 Android 第一阶段必须配置手机可访问的远程主接口和播放解析接口，不能使用 `127.0.0.1` 或 `localhost`，因为移动端不启动桌面内置 Node 音乐 API。
- `src-tauri/tauri.android.conf.json` 的 `beforeBuildCommand` 改为 Android 专用 Web 构建命令；桌面端仍保留原有 `build:web` / `build:desktop` 流程。
- 本轮不新增 iOS、后台播放、通知栏控制、商店发布、下载或本地音乐扫描能力，只补齐 Android 第一阶段生产构建的接口目标模式。

### 8.45 第二十一版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 构建目标模式新增问题。
- 已执行 `npm run check:android-env`；当前未创建 `.env.android`，脚本按预期阻止 Android 构建并提示缺少 `VITE_API`、`VITE_API_MUSIC`，避免误打包不可用 APK。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.46 第二十二版适配进度

- Android 环境变量检查继续加固：`scripts/check-android-env.mjs` 不只检查缺失值，也会拦截 `localhost`、`127.0.0.1`、`0.0.0.0` 和非法 URL，避免手机端误使用桌面本机地址导致首页、搜索、播放或歌词接口不可用。
- 检查脚本的中文提示改为 Unicode 转义文本，减少 Windows PowerShell 默认编码下出现乱码的概率，让后续配置 Android 目标模式时能看懂失败原因。
- `.gitignore` 新增 `.env.android`，真实 Android 远程接口配置不会被误提交；`.env.android.example` 继续保留为可提交模板，并补充真实配置文件已忽略的说明。
- 本轮不新增 iOS、后台播放、通知栏控制、商店发布、下载或本地音乐扫描能力，只补强 Android 第一阶段生产构建前的接口配置安全边界。

### 8.47 第二十二版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 环境变量校验新增问题。
- 已执行 `npm run check:android-env` 缺失配置场景；脚本按预期提示缺少 `VITE_API`、`VITE_API_MUSIC`。
- 已临时创建 `.env.android` 验证本地地址拦截；脚本按预期拦截 `http://127.0.0.1:30488` 和 `http://localhost:30489`，并确认 `.env.android` 被 `.gitignore` 忽略。临时文件已删除。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。

### 8.48 第二十三版适配进度

- Rust 原生命令层继续收敛 Android 第一阶段边界：`get_downloads_path` 在 mobile 目标下返回空路径，不再读取系统下载目录。
- Android 下 `write_local_file`、`write_local_text_file`、`delete_local_file` 和 `local_file_exists` 直接返回不支持、空结果或不存在，避免误触发下载、本地歌词保存和本地文件删除能力。
- Android 下 `minimize_window`、`close_window` 和 `quit_app` 改为安全空操作，避免移动端 WebView 误调用桌面窗口最小化、关闭或退出应用能力。
- 本轮不新增 iOS、后台播放、通知栏控制、商店发布、本地音乐扫描或下载能力，只补强 Tauri Android 启动期和基础页面链路的原生命令兜底。

### 8.49 第二十三版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 原生命令兜底新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.50 第二十四版适配进度

- Android 第一阶段继续收敛播放链路复杂度：移动端跳过“下一首歌曲”音频预加载，避免首阶段播放时并发拉取下一首 URL 和创建额外 Howl 实例。
- 当前歌曲播放链路保持不变，点击播放仍会按原流程获取歌曲详情、请求播放地址、创建当前音频实例并加载歌词候选。
- 桌面端仍保留下一首预加载能力；本轮不新增后台播放、通知栏控制、iOS、商店发布、本地音乐扫描或下载能力。

### 8.51 第二十四版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、动态导入与静态导入混用提示，不属于本轮 Android 跳过下一首预加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.52 第二十五版适配进度

- 歌曲下载入口继续收敛为桌面专属懒加载能力：`SongDownloadButton` 和 `useSongItem` 不再在组件初始化时静态加载下载 hook，只有桌面端用户点击下载按钮或右键下载菜单时才动态加载下载逻辑。
- 桌面右键歌曲菜单改为异步组件，Android 搜索结果、播放队列和首页歌曲项仍可复用同一套歌曲组件，但不会初始化右键下载菜单和下载组合式逻辑，减少移动端基础链路加载桌面专属模块的风险。
- 桌面端下载按钮展示、酷我音质选项、右键菜单下载歌词和默认下载行为保持不变；本轮不新增下载、本地音乐扫描、后台播放、通知栏控制、iOS 或商店发布能力。

### 8.53 第二十五版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；此前 `SongItemDropdown.vue` 同时动态/静态导入的提示已消失，构建仍有既有 chunk 体积、循环 chunk、`downloadQuality.ts` 等动态导入与静态导入混用提示，不属于本轮 Android 下载入口懒加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.54 第二十六版适配进度

- 搜索结果页、歌单/专辑详情页和收藏页的批量下载逻辑继续收敛为桌面端按需能力：页面初始化时不再静态加载 `useDownload`，只有桌面端用户点击批量下载时才动态加载下载 hook。
- 批量下载处理函数增加 `isDesktopRuntime` 兜底保护，即使未来模板误触发相关函数，Android 第一阶段也不会进入下载模块或桌面文件能力链路。
- 搜索、歌单和收藏页面的歌曲展示、播放全部、单曲播放、列表内搜索与滚动加载逻辑保持不变；本轮不新增下载、本地音乐扫描、后台播放、通知栏控制、iOS 或商店发布能力。

### 8.55 第二十六版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有 chunk 体积、循环 chunk、`downloadQuality.ts` 等动态导入与静态导入混用提示，不属于本轮批量下载按需加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.56 第二十七版适配进度

- 应用启动入口不再静态加载落雪音源 Worker 执行器：`App.vue` 仅在桌面端启用自定义落雪音源时动态导入 `LxMusicSourceRunner`，Android 第一阶段继续保留远端基础播放链路。
- 落雪音源解析策略增加非桌面运行时兜底：`LxMusicStrategy` 在 Android 下不参与可用策略筛选，即使误触发解析函数也会直接返回空结果。
- `LxMusicStrategy` 内部的 Worker 执行器改为真正解析时按需导入，避免 Android 播放主链路因为静态策略注册提前加载桌面自定义音源沙箱代码。
- 本轮不新增下载、本地音乐扫描、后台播放、通知栏控制、iOS 或商店发布能力，只继续收敛 Android 首页/搜索/播放/歌词基础链路的启动负担。

### 8.57 第二十七版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；`LxMusicSourceRunner` 已拆成按需 chunk，构建仍有既有 chunk 体积、循环 chunk 和动态导入与静态导入混用提示，不属于本轮落雪音源执行器懒加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.58 第二十八版适配进度

- 播放条里的桌面增强组件改为按需加载：`PlayBar` 不再静态导入重解析入口、歌曲下载按钮和高级控制菜单，只有桌面端渲染对应按钮时才加载这些组件。
- 歌曲项列表里的下载按钮改为异步组件：标准、列表和紧凑三种歌曲项不再随搜索结果、歌单列表初始化时静态加载下载按钮模块。
- 迷你播放条和简化播放条里的下载按钮也改为异步组件，桌面行为保持不变，Android 第一阶段继续只保留基础播放和歌词展示链路。
- 本轮不新增下载、本地音乐扫描、后台播放、通知栏控制、iOS 或商店发布能力，只减少 Android 首页/搜索/播放/歌词路径加载桌面下载、重解析和高级控制模块的机会。

### 8.59 第二十八版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；`SongDownloadButton`、`ReparsePopover` 和 `AdvancedControlsPopover` 已拆成按需 chunk，构建仍有既有 chunk 体积、循环 chunk 和动态导入与静态导入混用提示，不属于本轮播放组件懒加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.60 第二十九版适配进度

- `SongItem` 不再静态导入标准、列表、紧凑、首页和迷你五种歌曲项变体，改为根据实际布局按需加载，减少 Android 首页、搜索结果和播放队列初始化时解析暂未使用歌曲项组件的负担。
- `SearchItem` 内的 `MvPlayer` 改为异步组件，只有用户进入 MV 卡片场景时才加载 MV 播放器；Android 第一阶段的搜索歌曲、播放和歌词链路不再被 MV 播放器模块提前牵入。
- 本轮不新增 iOS、后台播放、通知栏控制、应用商店发布、下载或本地音乐扫描能力，只继续收敛 Android 第一阶段首页/搜索/播放/歌词基础路径的启动加载面。

### 8.61 第二十九版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；`HomeSongItem`、`MiniSongItem`、`ListSongItem`、`CompactSongItem`、`StandardSongItem` 和 `MvPlayer` 已拆成按需 chunk，构建仍有既有 chunk 体积、循环 chunk 和动态导入与静态导入混用提示，不属于本轮歌曲项和 MV 播放器懒加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.62 第三十版适配进度

- `PlayBar` 和 `MobilePlayBar` 不再常驻挂载全屏歌词包装器，只有用户展开全屏歌词时才加载 `MusicFullWrapper`，减少 Android 启动和基础播放条渲染时提前解析歌词全屏界面的负担。
- `MusicFullWrapper` 内部改为按当前设备形态异步加载桌面版或移动版歌词组件；Android 第一阶段进入歌词时只加载移动歌词界面，不再提前牵入桌面歌词设置、歌词修正和桌面全屏布局模块。
- 本轮不新增 iOS、后台播放、通知栏控制、应用商店发布、下载或本地音乐扫描能力，只继续收敛 Android 首页/搜索/播放/歌词基础链路的初始加载范围。

### 8.63 第三十版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；`MusicFullWrapper`、`MusicFullMobile` 和 `MusicFull` 已拆成按需 chunk，构建仍有既有 chunk 体积、循环 chunk 和动态导入与静态导入混用提示，不属于本轮全屏歌词懒加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.64 第三十一版适配进度

- `AppLayout` 不再静态导入桌面更新弹窗 `UpdateModal`，只有桌面运行时需要显示更新能力时才按需加载，Android 第一阶段启动路径不再提前解析更新弹窗、Markdown 渲染和桌面更新 API 相关逻辑。
- `SleepTimerTop` 改为异步组件，继续保留桌面/非移动布局的顶部定时器显示能力，但 Android 手机布局初始化时不再提前加载该增强 UI。
- 本轮不新增 iOS、后台播放、通知栏控制、应用商店发布、下载或本地音乐扫描能力，只继续收敛 Android 首页/搜索/播放/歌词基础路径的主布局加载面。

### 8.65 第三十一版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；`UpdateModal` 和 `SleepTimerTop` 已拆成按需 chunk，构建仍有既有 chunk 体积、循环 chunk 和动态导入与静态导入混用提示，不属于本轮主布局桌面增强组件懒加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.66 第三十二版适配进度

- `App.vue` 不再静态导入应用内快捷键模块，改为仅在桌面运行时进入主窗口后动态加载 `appShortcuts`，Android 第一阶段启动路径不再提前解析快捷键、快捷键提示和桌面 IPC 监听相关逻辑。
- 桌面端快捷键初始化仍复用原有 `initAppShortcuts` 和 `cleanupAppShortcuts`，在组件卸载时显式清理，避免改变桌面端全局快捷键和应用内快捷键行为。
- 本轮不新增 iOS、后台播放、通知栏控制、应用商店发布、下载或本地音乐扫描能力，只继续收敛 Android 首页、搜索、播放、歌词基础路径的首屏加载范围。

### 8.67 第三十二版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；`appShortcuts` 已拆成按需 chunk，构建仍有既有 chunk 体积、循环 chunk 和动态导入与静态导入混用提示，不属于本轮快捷键入口懒加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.68 第三十三版适配进度

- `SearchBar` 不再静态导入下载状态、应用更新状态和页面缩放 hook，改为通过 `SearchBarDesktopActions` 与 `SearchBarDesktopZoomRow` 两个桌面异步组件按需承载相关能力。
- Android 第一阶段进入主布局和搜索栏时只保留搜索输入、搜索建议、返回按钮、用户入口、主题切换等基础交互，不再提前解析下载监听、更新检查状态监听和桌面缩放 IPC 链路。
- 桌面端下载按钮、更新按钮和缩放菜单行继续保留原有行为，并通过搜索栏现有样式的深度选择器继承原视觉表现；本轮不新增 iOS、后台播放、通知栏控制、应用商店发布、下载或本地音乐扫描能力。

### 8.69 第三十三版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；`SearchBarDesktopActions` 与 `SearchBarDesktopZoomRow` 已拆成按需 chunk，构建仍有既有 chunk 体积、循环 chunk 和动态导入与静态导入混用提示，不属于本轮搜索栏桌面增强入口懒加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.70 第三十四版适配进度

- `StandardSongItem`、`ListSongItem` 和 `CompactSongItem` 中的 `SongDownloadButton` 改为桌面运行时才动态导入；Android 第一阶段渲染首页歌曲、搜索结果歌曲和播放列表歌曲时不再创建下载按钮模块入口。
- 非桌面运行时使用空组件兜底，保留歌曲项原有播放、加入下一首、更多菜单、收藏和列表展示交互，避免影响首页、搜索、播放、歌词基础路径。
- 本轮不新增 iOS、后台播放、通知栏控制、应用商店发布、下载或本地音乐扫描能力，只继续减少 Android 基础歌曲列表路径加载桌面下载模块的机会。

### 8.71 第三十四版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有循环 chunk、动态导入与静态导入混用和大 chunk 警告，不属于本轮歌曲项下载按钮桌面运行时加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。

### 8.72 第三十五版适配进度

- `PlayBar`、`MiniPlayBar` 和 `SimplePlayBar` 中的 `SongDownloadButton` 改为桌面运行时才动态导入；Android 第一阶段即使进入平板播放栏、迷你播放栏或歌词内简化播放栏，也不会创建下载按钮模块入口。
- 迷你播放栏和简化播放栏的下载按钮渲染条件补充 `isDesktopRuntime`，避免非桌面运行时只因为当前存在播放歌曲就尝试挂载下载能力。
- 桌面端下载按钮行为保持不变；本轮不新增 iOS、后台播放、通知栏控制、应用商店发布、下载或本地音乐扫描能力，只继续收敛 Android 播放和歌词基础路径里的桌面下载入口。

### 8.73 第三十五版自测记录

- 已通过 `npm run typecheck:web`。
- 已通过 `npm run build:web`；构建仍有既有循环 chunk、动态导入与静态导入混用和大 chunk 警告，不属于本轮播放栏下载按钮桌面运行时加载新增问题。
- 已执行 `git diff --check`；仅存在 Windows 行尾提示。
- 已执行 `npx tauri info`，Tauri 配置可正常读取；本机仍缺少 `rustc`、`Cargo`、`rustup`，因此暂时不能执行 `npx tauri android init/dev/build` 或 Android 真机启动验证。
