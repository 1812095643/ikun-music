# ikun-music 项目分析文档

## 1. 项目是做什么的

`ikun-music` 是一个桌面端音乐播放器项目，目标不是只做单一平台的在线播放，而是把下面几类能力整合到一个应用里：

- 网易云相关页面与账号能力：首页、搜索、歌单、专辑、排行榜、用户页、每日推荐、私人 FM 等。
- 多音源补链能力：当官方接口拿不到可播放地址时，走第三方解析、自定义 API 或 LX Music 脚本音源补齐播放链接。
- 本地音乐能力：扫描本地文件夹、解析音频元数据、缓存封面/歌词、直接播放本地文件。
- 桌面增强能力：系统托盘、迷你窗、独立歌词窗、快捷键、下载、应用更新、窗口控制。
- 跨运行时兼容：仓库同时保留了 Tauri 实现和 Electron 兼容层，当前构建主线偏向 Tauri。

从页面路由看，当前应用覆盖的主要页面包括：

- 首页：`/`
- 搜索：`/search`
- 歌单：`/list`
- 专辑：`/album`
- 排行榜：`/toplist`
- MV：`/mv`
- 播客：`/podcast`
- 历史/收藏：`/history`、`/favorite`
- 本地音乐：`/local-music`
- 下载管理：`/downloads`
- 设置：`/set`

## 2. 技术栈与整体架构

### 2.1 前端层

- `Vue 3`
- `Pinia`
- `Vue Router`
- `Naive UI`
- `Vite`
- `Howler.js`

前端入口在 `src/renderer/main.ts`，根组件在 `src/renderer/App.vue`。

### 2.2 桌面层

- `Tauri 2`：当前 `package.json` 的 `dev` / `build` 脚本都走 `tauri dev`、`tauri build`
- `Electron`：仓库仍保留 `src/main` 和 `electron.vite.config.ts`
- `tauriElectronCompat.ts`：把 Tauri API 适配成类似 Electron preload 的调用方式，降低两套运行时的差异

这意味着项目并不是“纯 Tauri”或“纯 Electron”，而是一个过渡中的双端兼容结构：

- 渲染层代码尽量只调用 `window.api`
- 具体由 Tauri 或 Electron 在底层提供窗口、文件、托盘、HTTP、存储等能力

### 2.3 内置本地服务

项目内置了一个本地音乐 API 服务，主要作用有两类：

- 提供网易云相关接口能力
- 提供本地扫描、补链解析等桌面场景接口

Electron 侧入口在 `src/main/server.ts`，Tauri 侧通过 `src-tauri/src/lib.rs` 拉起 `src-tauri/bin/alger-music-api.js`。

## 3. 关键目录说明

- `src/renderer`：Vue 前端页面、组件、状态、业务逻辑
- `src/main`：Electron 主进程能力，如窗口、托盘、快捷键、本地扫描、更新
- `src-tauri`：Tauri Rust 侧能力与打包配置
- `src/i18n`：多语言文案
- `resources`：图标、远程控制页面等资源文件
- `scripts`：打包与辅助脚本
- `docs`：项目内已有专项文档

## 4. 关键功能怎么实现

## 4.1 播放主链路

播放相关的核心职责分布比较清晰：

- `src/renderer/store/modules/player.ts`
  负责聚合播放、列表、收藏、定时器等多个子 store。
- `src/renderer/store/modules/playlist.ts`
  负责播放列表、顺序/循环/随机模式、上下首、预加载下一首。
- `src/renderer/store/modules/playerCore.ts`
  负责真正的“点歌到出声”主流程。
- `src/renderer/services/audioService.ts`
  负责音频实例、EQ、音量、倍速、音频输出设备和播放状态检测。
- `src/renderer/hooks/MusicHook.ts`
  负责歌词、进度、播放结束、独立歌词窗同步。

实际播放流程大致是：

1. 用户点击歌曲，进入 `playlist.setPlay()` 或 `playerCore.handlePlayMusic()`
2. `playerCore` 为这次播放创建 `requestId`，防止旧请求覆盖新请求
3. 如果歌曲详情不完整，会先补齐歌曲详情、封面、歌词等信息
4. 如果没有稳定的播放地址，就调用 `MusicParser.parseMusic()` 解析 URL
5. `preloadService` 预加载音频
6. `audioService.play()` 使用 `Howler` 建立音频实例
7. `MusicHook` 监听播放、暂停、结束、seek，驱动歌词和 UI 同步

这个链路里有两个设计点比较关键：

- 用 `playbackRequestManager` 避免快速切歌时的竞态覆盖
- 把歌词、取色等慢操作放到播放后补齐，优先保证“点了就能响”

## 4.2 多音源解析

多音源补链是这个项目最核心的业务功能之一，主入口在：

- `src/renderer/api/musicParser.ts`

它不是写死单一路径，而是按策略依次尝试，优先级大致如下：

1. `LxMusicStrategy`
2. 自定义 API
3. `gdmusic`
4. `unblockMusic`

对应关键文件：

- `src/renderer/api/lxMusicStrategy.ts`
- `src/renderer/api/parseFromCustomApi.ts`
- `src/renderer/api/musicParser.ts`
- `src/renderer/services/LxMusicSourceRunner.ts`
- `src/main/unblockMusic.ts`

其中：

- LX Music 脚本音源通过 `Worker` 沙箱执行，避免用户脚本直接跑在主线程
- 自定义 API 通过导入 JSON 配置接入第三方解析服务
- `unblockMusic` 通过第三方平台补链，默认会过滤并组合可用音源
- 解析结果会做缓存，失败结果也会短时间缓存，避免重复打失败接口

这套机制解释了为什么项目能同时支持：

- 官方可播就直接播
- 官方不可播时自动补链
- 手动切换音源重新解析当前歌曲

## 4.3 本地音乐扫描与缓存

本地音乐链路分为“主进程扫描”和“前端缓存管理”两部分。

主进程侧：

- `src/main/modules/localMusicScanner.ts`

负责：

- 递归扫描目录
- 过滤支持的音频格式
- 用 `music-metadata` 解析标题、歌手、专辑、时长、封面、歌词
- 返回文件修改时间，用于增量扫描

前端侧：

- `src/renderer/store/modules/localMusic.ts`

负责：

- 记录用户添加的文件夹
- 调用 `window.api.scanLocalMusicWithStats()`
- 只对新增或修改过的文件重新解析元数据
- 把结果写入 IndexedDB，避免每次都全量扫描

这里的实现思路比较实用：

- 文件系统遍历放在桌面端能力层做
- 元数据结果放在前端 IndexedDB 做持久缓存
- 通过 `modifiedTime` 实现增量刷新

## 4.4 歌词窗、托盘、迷你窗

桌面增强能力主要由下面几个模块协作：

- `src/renderer/hooks/MusicHook.ts`
- `src/renderer/App.vue`
- `src/main/modules/tray.ts`
- `src/main/modules/shortcuts.ts`
- `src/main/lyric.ts`
- `src-tauri/src/lib.rs`

实现方式大致是：

- 前端播放状态变化后，同步调用 `window.api.updateTrayState()`
- 桌面层重建托盘菜单和 tooltip
- 歌词窗通过单独窗口承载，主窗口持续推送当前歌词、时间和播放状态
- 快捷键和托盘按钮最后仍然回到播放器 store 的统一动作上

这部分说明项目不是“网页套壳”级别，而是做了明显的桌面化交互增强。

在当前实现里，多窗口路由是桌面能力落地的关键约束：

- 主窗口走常规业务路由，例如首页、搜索、歌单、本地音乐等页面
- 迷你模式使用独立的 `/mini` 路由，由 `MiniLayout` 承载，主窗口通过 `mini-mode` 事件在普通布局与迷你布局之间切换
- 桌面歌词使用独立的 `/lyric` 路由，歌词窗口只负责展示和交互，不承载主业务页面
- 托盘面板使用 `/tray-panel` 路由，承担轻量播放控制与状态查看

从职责拆分看，迷你模式和桌面歌词各自承担不同的桌面交互目标：

- `App.vue` 负责识别当前窗口类型，并根据窗口类型决定是否初始化完整业务能力
- `MiniPlayBar.vue` 负责迷你模式下的播放控制、播放列表展开与窗口恢复
- `MusicHook.ts` 负责把播放进度、歌词索引和播放状态同步到独立歌词窗口
- `src/main/lyric.ts` 与 `src-tauri/src/lib.rs` 分别承接 Electron 与 Tauri 下的歌词窗口能力
- `tauriElectronCompat.ts` 负责把窗口事件、歌词数据推送和窗口控制适配成统一调用入口

## 4.5 Tauri / Electron 兼容层

兼容层关键文件：

- `src/renderer/utils/tauriElectronCompat.ts`

它做了几件事：

- 统一 `window.api`、`ipcRenderer` 风格调用
- 在 Tauri 环境下映射窗口控制、文件选择、HTTP 请求、下载、存储
- 在 Tauri 下补齐迷你模式、托盘面板、桌面歌词这些多窗口事件通道
- 在浏览器或非桌面环境下做有限兜底

这层的价值是：

- 渲染层业务代码不必直接区分 Tauri 和 Electron
- 后续替换桌面运行时的代价更低

## 5. 我对这个项目的整体判断

这是一个“桌面音乐播放器 + 多音源补链平台 + 本地音乐管理器”的组合型项目。

它的业务核心不是简单页面，而是下面三件事：

- 稳定地拿到可播放 URL
- 稳定地把音频播放状态、歌词、托盘、快捷键同步起来
- 在桌面环境里把本地文件和应用窗口能力接进来

如果你后续要继续维护，这几个模块最值得优先熟悉：

- `src/renderer/store/modules/playerCore.ts`
- `src/renderer/services/audioService.ts`
- `src/renderer/api/musicParser.ts`
- `src/renderer/store/modules/localMusic.ts`
- `src/main/modules/localMusicScanner.ts`
- `src/renderer/utils/tauriElectronCompat.ts`

#
