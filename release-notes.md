# ikun音乐 5.3.13

- 导入歌单页面改为固定工作区，列表在容器内部滚动，避免页面滚动遮挡播放器。
- 平板和车机使用左右布局，左侧显示来源统计与导入步骤，右侧显示歌曲清单和匹配结果。
- 手机端动态扣除顶部区域、操作栏和底部播放器高度，保证匹配操作始终可见。

# ikun音乐 5.3.12

- 修正播放栏音律条位置，使其贴合进度条上方且不占用播放空间。
- 下载按钮调整为与播放列表一致的透明无边框图标样式。

ikun音乐 5.3.11

- 精简模式收紧底部窗口高度，移除多余留白；托盘播放面板同步收紧并保留正常内边距。
- 桌面播放栏、精简模式、完整播放页和托盘面板加入左右声道音律条；可用时读取真实频谱，跨域音源继续正常播放并使用轻量视觉回退。
- 频谱采样按需启动，暂停、隐藏和关闭面板后停止，避免增加持续 CPU 占用。

- “导入歌单”移至右上角横杠菜单，在任意桌面页面均可进入。
- 导入页面保留顶部主导航，移除返回按钮，直接通过导航切换页面。
- 修复离开导入页后音乐库白屏：为路由过渡提供稳定根容器，避免退出动画阻塞后续页面渲染。
- 其他页面在没有站内历史记录时，返回操作自动回到首页。

本次通过 GitHub Actions 构建 Windows x64、Windows ARM64、Mac Intel 和 Mac Apple 芯片四种架构。Windows 提供安装版和便携版，Mac 提供 DMG 安装包及 `.app.tar.gz` 更新文件；自动更新按系统、架构和安装形式匹配。

Windows 便携版须完整解压并保留 runtime 目录。Mac 要求 macOS 13.5 或更高版本，目前使用临时签名、未完成苹果公证，尚未进行 Mac 真机完整播放与桌面歌词验收。

5.3.6 及以上版本可通过应用内检查更新升级；5.3.5 及以前版本需从 GitHub 手动安装一次。安卓手机、平板和车机安装包见独立的 [Android 0.1.6 发布页](https://github.com/1812095643/ikun-music/releases/tag/mobile-v0.1.6)。
