ikun音乐 5.3.0

- 移除旧桌面主进程、预加载层与废弃构建配置，统一使用 Tauri 桌面服务。
- 音乐协议通过私有标准输入输出管道调用，桌面端不再占用固定 HTTP 业务端口。
- 音乐服务按需启动、并发请求隔离，应用退出时自动结束随包服务。
- 修复 Windows 资源路径导致服务启动退出，以及 HTTP 插件版本不匹配导致首页一直加载的问题。
- 使用小体积 Tauri 主程序与完整随包运行时，安装版一次安装；便携版完整解压后运行。
- 启动自动检查新版本并提示，支持签名验证后的安装包和便携包更新。
- 保留现有 Android 平台隔离、播放器、歌词及用户配置。

旧版升级：5.1.x / 5.2.x 请下载完整安装包或完整便携 ZIP。不要只替换 EXE；本版本开始使用随包 runtime 目录。
